// write a program that checks whether a number is divisible by both 2 & 3, but not 7
// using logical operators only

#include <stdio.h>

int main() {

  int a, b, c;

  printf("insert an integer(a): ");
  scanf("%d", &a);

  printf("insert an integer(b): ");
  scanf("%d", &b);

  

  
return 0;
}
